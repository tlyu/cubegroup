[package]
name = "cubegroup"
version = "0.1.0"
edition = "2024"
license = "BSD-2-Clause"
autoexamples = true

[dependencies]
bytemuck = { version = "1.25.0", features = ["aarch64_simd", "derive", "must_cast"] }

[features]
array = []

[dev-dependencies]
clap = "4.6.1"
